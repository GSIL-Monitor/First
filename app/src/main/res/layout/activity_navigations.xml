<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.design.widget.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#555555"
        app:layout_constraintBottom_toBottomOf="parent"/>
    <FrameLayout
        android:id="@+id/flContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottomNavigation">
        <!--<fragment-->
            <!--android:id="@+id/rootFragment"-->
            <!--android:layout_width="match_parent"-->
            <!--android:layout_height="match_parent"-->
            <!--android:name="androidx.navigation.fragment.NavHostFragment"-->
            <!--app:defaultNavHost="true"-->
            <!--app:navGraph="@navigation/navigations_graph" />-->
    </FrameLayout>
    <Button
        android:id="@+id/btnHandler"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:text="add"/>
    <Button
        android:id="@+id/btnRemove"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:text="remove"
        app:layout_constraintTop_toBottomOf="@id/btnHandler"/>
    <Button
        android:id="@+id/btnHandlerAll"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:text="add all"
        app:layout_constraintTop_toBottomOf="@id/btnRemove"/>
    <Button
        android:id="@+id/btnRemoveAll"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:text="remove all"
        app:layout_constraintTop_toBottomOf="@id/btnHandlerAll"/>
</android.support.constraint.ConstraintLayout>